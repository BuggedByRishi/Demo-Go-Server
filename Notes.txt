- Module path : rishi.net  (used to define third part packages).

- Three main essentials : 

  (Writes HTTP response)                    1. Handeler : Handelers are responsible for executing the program logic and writing HTTP response header and bodies.

  (Maps the URL's with the Handeler)        2. Router(servmux) : This maps the URL patterns and the handlers, One servmux has all the routes for your application.

  (Listerns from the incomming request)     3. Web Server : We can establish Web Server and and listen from the incomming request 

- err := http.ListenAndServe(":8080", mux)  In this the format to access a specific thing we should use the format as host:port
    if not them the post return all the responses.

- Go servemux supports two types of URl's : 
    1. Fixed path : does not end with a /
    2. Subtree path : ends with  a /

- Used w.Header().Set() to add a new header to the response header.

- cmd is the main executable directory.

- pg_dump : This is a command utility which is used to backup the database and export all the database schemas(tables, views, functions)

- hhtp.StripPrefix is a built-in http middleware which is used to srtip the prefix form the URL before passing the request to another

